# TODO: Create a proper SVG icon using Inkscape
icon_name = '@0@.png'.format(base_id)

install_data(
  icon_name,
  install_dir: datadir / 'icons' / 'hicolor' / 'scalable' / 'apps',
  rename: '@0@.png'.format(application_id),
)
